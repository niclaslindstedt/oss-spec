{
  "name": "{{ name }}-website",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "node scripts/extract-source-data.mjs",
    "dev": "npm run extract && vite",
    "build": "npm run extract && vite build && node scripts/generate-seo.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5"
  }
}
