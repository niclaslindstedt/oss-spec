<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{ name }}</title>
    <meta name="description" content="{{ description }}" />
  </head>
  <body>
    <h1>{{ name }}</h1>
    <p>{{ description }}</p>
  </body>
</html>
